#include<stdio.h>
#include<math.h>
int main(int arg,char *args[])
{
    int n=atoi(args[1]),i=0,dec=0;
    while(n>0)
    {
        dec=dec+(n%10)*pow(2,i++);
        n=n/10;
    }
    printf("%d",dec);
}
